[user]
	name = Garrett Robinson
	email = garrettr@riseup.net
[alias]
	co = checkout
    hgp = show --format=\"From: %an <%ae>%n%s%n%b\" -U8
[core]
	editor = vim
[diff]
    renames = copy

[color]
    branch = auto
    diff = auto
    status = auto

[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green

[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red bold
    new = green bold

[color "status"]
    added = yellow
    changed = green
    untracked = cyan

[credential]
	helper = cache --timeout=3600
